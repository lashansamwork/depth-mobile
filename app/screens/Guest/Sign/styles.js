import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  buttonWrapper: {
    marginTop: 20,
    display: 'flex',
    flexDirection: 'row',
    justifyContent: 'flex-end',
  },
});
